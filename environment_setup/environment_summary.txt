Canonical local environment
===========================

Environment name: vc_sws3026
Python: 3.11
Execution target: CPU-first; CUDA is optional

Installation:
1. conda env create -f environment_setup\environment.yml
2. conda activate vc_sws3026
3. .\environment_setup\install_runtime.ps1

Required runtime invariant:
- opencv-contrib-python is installed.
- opencv-python and opencv-python-headless are not installed.
- cv2.face.createFacemarkLBF is available.
- Ultralytics is installed with --no-deps after its declared runtime
  dependencies, preventing pip from adding a conflicting OpenCV wheel.

Verification:
.\environment_setup\install_runtime.ps1 -CheckOnly
.\check_merge.ps1

The versioned requirements-lock.txt records one tested dependency snapshot.
The check_runtime.py script is the authoritative conflict and capability check
for the active environment; no machine-specific paths are assumed.
